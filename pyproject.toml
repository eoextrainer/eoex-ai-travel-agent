[tool.black]
line-length = 100
target-version = ["py311"]
include = '\.pyi?$'
exclude = [
  '/(\.eggs|\.git|\.hg|\.mypy_cache|\.nox|\.tox|\.venv|venv|_build|buck-out|build|dist)/',
]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
addopts = "-q"
